{
    "name": "deploy",
    "type": "module",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "author": "@rustytsuki",
    "license": "BSD-3-CLAUSE",
    "scripts": {
        "fetch_kernel": "node index.js --fetch",
        "build:win": "node index.js -t x86_64-pc-windows-msvc",
        "publish:win": "node index.js -t x86_64-pc-windows-msvc -p",
        "build:linux:rpm": "node index.js -t x86_64-unknown-linux-gnu --pkg rpm -p",
        "build:linux:deb": "node index.js -t x86_64-unknown-linux-gnu --pkg deb -p",
        "build:site": "node index.js -s -t wasm32-unknown-emscripten",
        "publish:site": "node index.js -s -t wasm32-unknown-emscripten -p"
        
    },
    "devDependencies": {
        "adm-zip": "^0.5.16",
        "command-line-args": "^5.2.1",
        "follow-redirects": "^1.15.9",
        "fs-extra": "^11.3.0",
        "octokit": "^4.1.3",
        "tar": "^7.4.3"
    }
}
